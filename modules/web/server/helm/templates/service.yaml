apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubely.fullname" . }}
  labels:
    {{- include "kubely.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kubely.selectorLabels" . | nindent 4 }}
